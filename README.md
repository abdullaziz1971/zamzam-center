# دليل استخدام وتحديث نظام العروض والطلبات

## 🚀 مقدمة

أهلاً بك في نظام إدارة العروض والطلبات المطور لموقع "زمزم جملة". هذا الدليل يشرح لك كيف تدير وتحدث كل شيء بسهولة من خلال ملف واحد مركزي (`data.js`).

## 📁 بنية الملفات

- `index.html`: الصفحة الرئيسية (لا تحتاج لتعديلها).
- `companies.html`: صفحة عرض الشركات (لا تحتاج لتعديلها).
- `order.html`: صفحة طلب الشراء (لا تحتاج لتعديلها).
- `data.js`: **الملف المركزي الذي ستعمل عليه**. يحتوي على كل العروض والمنتجات.
- `cart.js`: نظام السلة (لا تحتاج لتعديله).
- `logo.png`: شعار الموقع.
- `.htaccess`: ملف توجيه (لا تحتاج لتعديله).

## 🛠️ كيفية تحديث العروض والأسعار (data.js)

كل ما عليك فعله هو فتح ملف `data.js` وتعديل البيانات مباشرة. النظام سيقوم بتحديث كل الصفحات تلقائياً.

### 1. تحديث المؤقت الزمني للعروض

لتغيير تاريخ انتهاء العروض المدمجة والمميزة، عدّل `endDate`:

```javascript
const ZAMZAM_DATA = {
  endDate: "2025-10-21T23:59:59", // غيّر هذا التاريخ (YYYY-MM-DDTHH:mm:ss)
  // ...
};
```

### 2. تحديث العروض المدمجة

اذهب إلى قسم `mergedOffers` وأضف أو عدّل المنتجات. كل منتج هو كائن (`{}`) له `id` فريد، `title`، و `price`.

```javascript
mergedOffers: {
  items: [
    { id: "merge-01", title: "كاميو إيدي 500 مل ×12 + سار باكيتات 120 غ ×48", price: 15.00 },
    { id: "merge-02", title: "مسحوق جزيرة 440 غ ×13 + سار باكيتات 120 غرام×48", price: 15.75 },
    // ... أضف المزيد هنا
  ]
},
```

### 3. تحديث العروض المميزة

اذهب إلى `featuredOffers`. لاحظ وجود `price` (السعر الأصلي) و `discountedPrice` (السعر المميز).

```javascript
featuredOffers: {
  items: [
    { id: "feat-01", title: "فوط بيبي لايف ستاندرد (1–7)", description: "تعبئة 4 حبات", price: 18.50, discountedPrice: 18.25 },
    { id: "feat-02", title: "أودكس كلور", description: "1 لتر ×15", price: 5.50, discountedPrice: 4.85 },
    // ... أضف المزيد هنا
  ]
},
```

### 4. إدارة الشركات ومنتجاتها

- **لإضافة شركة جديدة:** انسخ كائن شركة موجود، غيّر `id`, `name`, `icon`, `description` و `products`.
- **لإخفاء شركة مؤقتاً:** غيّر `active: true` إلى `active: false`.
- **لترتيب ظهور الشركات:** عدّل قيمة `displayOrder`.

```javascript
companies: [
  {
    id: "petra",
    name: "شركة البتراء",
    icon: "🧴",
    description: "حتى نفاد الكمية",
    active: true, // للتحكم بظهور الشركة
    displayOrder: 1, // للترتيب
    products: [
      // ... منتجات الشركة
    ]
  },
  // ... أضف شركات جديدة هنا
]
```

### 5. إضافة منتجات للشركات

داخل `products` لكل شركة، يمكنك إضافة المنتجات.

#### منتج عادي:

```javascript
{ 
  id: "petra-01", 
  title: "تايد باكيتات 130 غ", 
  packaging: "48 حبة", 
  price: 10.55, 
  category: "مساحيق" 
}
```

#### منتج غير متوفر:

اجعل `price` يساوي `null`.

```javascript
{ 
  id: "petra-28", 
  title: "جيليت 2 بلس حساس", 
  price: null, // سيظهر "غير متوفر"
  category: "شفرات" 
}
```

#### منتج متعدد التعبئة (مثل شفرات جيليت):

- أضف `hasVariants: true`.
- أضف مصفوفة `variants` تحتوي على التعبئات المختلفة.

```javascript
{
  id: "petra-29",
  title: "جيليت 2 (44+4)",
  category: "شفرات",
  hasVariants: true,
  variants: [
    { label: "سعر باكيت", price: 12.00 },
    { label: "الكرتونة (12 كرت)", price: 68.00 }
  ]
}
```

## ✨ الميزات التلقائية

1.  **حفظ السلة:** طلب الشراء يُحفظ تلقائياً في متصفح المستخدم. إذا أغلق الصفحة وعاد، سيجد طلبه كما هو.
2.  **تزامن الأسعار:** إذا غيّرت سعراً في `data.js`، سيتم تحديثه تلقائياً في سلة المستخدم عند زيارته التالية، مع إشعار يوضح التغيير.
3.  **إدارة الشركات:** يمكنك إضافة عدد لا نهائي من الشركات، والنظام سيعرضها وينظمها تلقائياً.

## 📞 الدعم الفني

إذا واجهت أي مشاكل، لا تتردد في التواصل معنا.

