# 🚀 دليل التثبيت - لوحة تحكم زمزم جملة

## 📋 المحتويات

1. [المتطلبات](#المتطلبات)
2. [رفع الملفات إلى Hostinger](#رفع-الملفات-إلى-hostinger)
3. [التأكد من التثبيت](#التأكد-من-التثبيت)
4. [الإعدادات الأولية](#الإعدادات-الأولية)
5. [استيراد البيانات الحالية](#استيراد-البيانات-الحالية)
6. [حل المشاكل](#حل-المشاكل)

---

## 📦 المتطلبات

قبل البدء، تأكد من توفر:

- ✅ حساب استضافة Hostinger نشط
- ✅ الوصول إلى hPanel (لوحة تحكم Hostinger)
- ✅ موقع `zamzam-center.com` موجود ونشط
- ✅ ملف `zamzam-admin.zip` (الذي سأرفقه لك)

---

## 📤 رفع الملفات إلى Hostinger

### الطريقة 1: عبر File Manager (الأسهل)

#### الخطوة 1: تسجيل الدخول إلى Hostinger

1. اذهب إلى [hpanel.hostinger.com](https://hpanel.hostinger.com)
2. سجل دخولك بحسابك

#### الخطوة 2: فتح File Manager

1. من القائمة الجانبية، اختر موقعك `zamzam-center.com`
2. اضغط على **File Manager**

#### الخطوة 3: رفع ملفات لوحة التحكم

1. في File Manager، اذهب إلى مجلد `public_html`
2. اضغط على زر **Upload** (أعلى الصفحة)
3. اختر ملف `zamzam-admin.zip` من جهازك
4. انتظر حتى يكتمل الرفع
5. بعد الرفع، انقر بزر الماوس الأيمن على الملف المضغوط
6. اختر **Extract** لفك الضغط
7. سيتم إنشاء مجلد `admin` يحتوي على جميع ملفات لوحة التحكم
8. احذف ملف `zamzam-admin.zip` بعد فك الضغط

#### الخطوة 4: التأكد من البنية

بعد فك الضغط، يجب أن تكون البنية كالتالي:

```
public_html/
├── index.html (الموقع الحالي)
├── order.html
├── data.js
├── cart.js
├── logo.png
├── admin/
│   ├── admin.html
│   ├── admin-script.js
│   ├── admin-styles.css
│   ├── config.js
│   └── data.js (نسخة من ملف البيانات)
```

---

### الطريقة 2: عبر FTP (للمحترفين)

إذا كنت تفضل استخدام برنامج FTP:

#### الخطوة 1: الحصول على بيانات FTP

1. في hPanel، اذهب إلى **Files → FTP Accounts**
2. انسخ بيانات الاتصال:
   - **Host:** ftp.zamzam-center.com
   - **Username:** (اسم المستخدم)
   - **Password:** (كلمة المرور)
   - **Port:** 21

#### الخطوة 2: الاتصال عبر FileZilla

1. حمّل وثبّت [FileZilla](https://filezilla-project.org/) إذا لم يكن لديك
2. افتح FileZilla
3. أدخل بيانات الاتصال في الأعلى:
   - Host: `ftp.zamzam-center.com`
   - Username: (اسم المستخدم)
   - Password: (كلمة المرور)
   - Port: `21`
4. اضغط **Quickconnect**

#### الخطوة 3: رفع الملفات

1. في الجانب الأيسر (جهازك)، اذهب إلى مجلد `zamzam-admin` (بعد فك الضغط على جهازك)
2. في الجانب الأيمن (السيرفر)، اذهب إلى `public_html`
3. اسحب مجلد `admin` بالكامل من الجانب الأيسر إلى الجانب الأيمن
4. انتظر حتى يكتمل الرفع

---

## ✅ التأكد من التثبيت

بعد رفع الملفات، تأكد من أن كل شيء يعمل:

### 1. فتح لوحة التحكم

1. افتح المتصفح
2. اذهب إلى: `https://www.zamzam-center.com/admin/admin.html`
3. يجب أن تظهر صفحة تسجيل الدخول

### 2. تسجيل الدخول

1. أدخل كلمة المرور الافتراضية: `zamzam2025`
2. اضغط **دخول**
3. يجب أن تظهر لوحة التحكم

### 3. التأكد من الأقسام

تأكد من ظهور جميع الأقسام:
- ✅ لوحة المعلومات
- ✅ العروض المدمجة
- ✅ العروض المميزة
- ✅ الشركات
- ✅ المنتجات
- ✅ الإعدادات

---

## ⚙️ الإعدادات الأولية

بعد التأكد من التثبيت، قم بالإعدادات الأولية:

### 1. تغيير كلمة المرور

**مهم جداً!** غيّر كلمة المرور الافتراضية فوراً:

1. من القائمة الجانبية، اضغط على **⚙️ الإعدادات**
2. في قسم **🔐 تغيير كلمة المرور**:
   - أدخل كلمة المرور الجديدة (6 أحرف على الأقل)
   - أعد إدخالها للتأكيد
3. اضغط **💾 حفظ كلمة المرور**
4. **احفظ كلمة المرور الجديدة في مكان آمن!**

### 2. تحديث رقم الواتساب

تأكد من أن رقم الواتساب صحيح:

1. في قسم **📱 رقم الواتساب**:
   - تأكد من الرقم: `962799755536`
   - إذا كان خاطئاً، عدّله (بدون + أو 00)
2. اضغط **💾 حفظ الرقم**

### 3. تحديد تاريخ انتهاء العروض

1. في قسم **⏰ تاريخ انتهاء العروض**:
   - اختر التاريخ (عادةً 3 أيام من الآن)
   - اختر الوقت (عادةً 23:59)
2. اضغط **💾 حفظ التاريخ**

---

## 📥 استيراد البيانات الحالية

إذا كان لديك بيانات موجودة في الموقع الحالي، يمكنك استيرادها:

### الطريقة التلقائية (موصى بها)

لوحة التحكم ستقرأ البيانات تلقائياً من ملف `data.js` الموجود في الموقع.

**لا تحتاج لفعل أي شيء!**

### الطريقة اليدوية (إذا لم تعمل التلقائية)

1. افتح File Manager في Hostinger
2. اذهب إلى `public_html`
3. افتح ملف `data.js` (Edit)
4. انسخ المحتوى بالكامل
5. اذهب إلى `public_html/admin`
6. افتح ملف `data.js` (Edit)
7. الصق المحتوى واحفظ

---

## 🔗 ربط لوحة التحكم بالموقع

لوحة التحكم مرتبطة تلقائياً بالموقع عبر ملف `data.js` المشترك.

### كيف يعمل؟

1. عندما تحفظ أي تعديل في لوحة التحكم، يتم تحديث ملف `data.js`
2. الموقع يقرأ من نفس الملف
3. التغييرات تظهر تلقائياً في الموقع

### التأكد من الربط

1. في لوحة التحكم، أضف عرض مدمج تجريبي
2. احفظ
3. افتح الموقع: `https://www.zamzam-center.com`
4. يجب أن تظهر العرض الجديد

---

## 🛡️ الأمان

### 1. حماية لوحة التحكم

لوحة التحكم محمية بكلمة مرور، لكن يمكنك إضافة حماية إضافية:

#### حماية المجلد بكلمة مرور (اختياري)

1. في hPanel، اذهب إلى **Advanced → Directory Privacy**
2. اختر مجلد `admin`
3. فعّل الحماية
4. أنشئ اسم مستخدم وكلمة مرور
5. احفظ

الآن، عند فتح لوحة التحكم، سيُطلب منك:
- اسم المستخدم وكلمة المرور (حماية المجلد)
- ثم كلمة مرور لوحة التحكم

### 2. النسخ الاحتياطي

**مهم جداً!** احفظ نسخة احتياطية من ملف `data.js` بانتظام:

#### الطريقة 1: عبر لوحة التحكم

1. افتح لوحة التحكم
2. اضغط على **💾 نسخ احتياطي**
3. اضغط **📥 تحميل نسخة احتياطية**
4. احفظ الملف على جهازك

#### الطريقة 2: عبر File Manager

1. في File Manager، اذهب إلى `public_html`
2. انقر بزر الماوس الأيمن على `data.js`
3. اختر **Download**
4. احفظ الملف على جهازك

---

## 🔧 حل المشاكل

### المشكلة 1: صفحة تسجيل الدخول لا تظهر

**الأسباب المحتملة:**
- الملفات لم تُرفع بشكل صحيح
- المسار خاطئ

**الحل:**
1. تأكد من أن الملفات موجودة في `public_html/admin/`
2. جرّب الرابط: `https://www.zamzam-center.com/admin/admin.html`
3. تأكد من أن ملف `admin.html` موجود

### المشكلة 2: كلمة المرور لا تعمل

**الأسباب المحتملة:**
- كلمة المرور خاطئة
- ملف `config.js` لم يُرفع

**الحل:**
1. جرّب كلمة المرور الافتراضية: `zamzam2025`
2. إذا لم تعمل، افتح File Manager
3. اذهب إلى `public_html/admin/`
4. افتح ملف `config.js` (Edit)
5. ابحث عن السطر:
   ```javascript
   password: "zamzam2025",
   ```
6. تأكد من أن كلمة المرور صحيحة

### المشكلة 3: التغييرات لا تظهر في الموقع

**الأسباب المحتملة:**
- التخزين المؤقت (Cache)
- ملف `data.js` لم يتم تحديثه

**الحل:**
1. امسح الكاش في المتصفح (Ctrl + F5)
2. إذا كان Cloudflare مفعّل، امسح الكاش من لوحة تحكم Cloudflare
3. تأكد من أن ملف `data.js` في `public_html` تم تحديثه

### المشكلة 4: لوحة التحكم بطيئة

**الأسباب المحتملة:**
- الاتصال بالإنترنت بطيء
- عدد كبير من البيانات

**الحل:**
1. تأكد من اتصالك بالإنترنت
2. جرّب متصفحاً آخر (Chrome أو Firefox)
3. امسح الكاش والبيانات المخزنة

### المشكلة 5: خطأ "Permission Denied"

**الأسباب المحتملة:**
- صلاحيات الملفات خاطئة

**الحل:**
1. في File Manager، انقر بزر الماوس الأيمن على مجلد `admin`
2. اختر **Permissions**
3. اضبط الصلاحيات إلى `755` للمجلدات و `644` للملفات

---

## 📞 الدعم

إذا واجهت أي مشكلة لم تُذكر هنا:

1. **راجع دليل الاستخدام** (`ADMIN_GUIDE.md`)
2. **تأكد من اتباع جميع الخطوات** في هذا الدليل
3. **احفظ نسخة احتياطية** قبل أي محاولة لحل المشكلة
4. **تواصل معي** - سأكون سعيداً بمساعدتك

---

## ✅ قائمة التحقق النهائية

قبل البدء في استخدام لوحة التحكم، تأكد من:

- ✅ جميع الملفات مرفوعة في `public_html/admin/`
- ✅ صفحة تسجيل الدخول تظهر بشكل صحيح
- ✅ تم تغيير كلمة المرور الافتراضية
- ✅ رقم الواتساب صحيح
- ✅ تاريخ انتهاء العروض محدّث
- ✅ تم حفظ نسخة احتياطية أولية
- ✅ التغييرات تظهر في الموقع

---

## 🎉 تهانينا!

لوحة التحكم الآن جاهزة للاستخدام! 

يمكنك البدء في إدارة موقعك بسهولة وسرعة. راجع **دليل الاستخدام** (`ADMIN_GUIDE.md`) لمعرفة كيفية استخدام جميع الميزات.

**نتمنى لك تجربة ممتعة! 🚀**

---

*آخر تحديث: أكتوبر 2025*
*الإصدار: 1.0*

